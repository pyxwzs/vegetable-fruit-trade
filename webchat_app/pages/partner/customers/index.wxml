<view class="customer-page">
  <view class="summary-card">
    <view class="summary-head">
      <text class="summary-title">客户概览</text>
      <view class="add-btn" bindtap="openCreate">+ 新增</view>
    </view>
    <view class="summary-metrics">
      <view class="metric-item">
        <text class="metric-label">客户总数</text>
        <text class="metric-value accent">{{ totalCount }}</text>
      </view>
      <view class="metric-divider" />
      <view class="metric-item">
        <text class="metric-label">启用中</text>
        <text class="metric-value green">{{ activeCount }}</text>
      </view>
    </view>
  </view>

  <view class="toolbar">
    <view class="search-box">
      <text class="search-icon">🔍</text>
      <input
        class="search-input"
        value="{{ keyword }}"
        placeholder="搜索名称/联系人"
        confirm-type="search"
        bindinput="onSearchInput"
        bindconfirm="onSearchSubmit"
      />
      <text wx:if="{{ keyword }}" class="search-clear" bindtap="onSearchClear">×</text>
    </view>
    <picker
      mode="selector"
      range="{{ filters }}"
      range-key="label"
      value="{{ filterIndex }}"
      bindchange="onFilterChange"
    >
      <view class="filter-select">
        <text>{{ filters[filterIndex].label }}</text>
        <text class="filter-arrow">▾</text>
      </view>
    </picker>
  </view>

  <view wx:if="{{ loading && !list.length }}" class="state-box">
    <t-loading theme="circular" size="48rpx" />
  </view>

  <view wx:elif="{{ !list.length }}" class="state-box">
    <t-empty icon="user" description="暂无客户" />
  </view>

  <view wx:else class="card-list">
    <view wx:for="{{ list }}" wx:key="id" class="customer-card">
      <view class="card-head">
        <view class="head-main">
          <view class="customer-name">{{ item.name }}</view>
          <view class="customer-meta">{{ item.contact }}</view>
        </view>
        <view class="head-right">
          <t-tag theme="{{ item.statusTheme }}" variant="light" size="small">{{ item.statusText }}</t-tag>
        </view>
      </view>

      <view class="card-rows">
        <view class="info-row">
          <text class="k">电话</text>
          <text
            class="v {{ item.phone !== '—' ? 'link' : '' }}"
            data-phone="{{ item.phone }}"
            catchtap="onCallPhone"
          >{{ item.phone }}</text>
        </view>
        <view wx:if="{{ item.address }}" class="info-row">
          <text class="k">地址</text>
          <text class="v">{{ item.address }}</text>
        </view>
      </view>

      <view class="card-actions" catchtap="stopBubble">
        <t-button
          size="small"
          theme="primary"
          variant="outline"
          data-id="{{ item.id }}"
          catchtap="openEdit"
        >编辑</t-button>
        <t-button
          size="small"
          theme="danger"
          variant="outline"
          data-id="{{ item.id }}"
          data-name="{{ item.name }}"
          catchtap="onDelete"
        >删除</t-button>
      </view>
    </view>

    <view class="pagination" wx:if="{{ total > 0 }}">
      <text class="pg-total">共 {{ total }} 条</text>
      <picker
        mode="selector"
        range="{{ pageSizeOptions }}"
        value="{{ pageSizeIndex }}"
        bindchange="onPageSizeChange"
      >
        <view class="pg-size">{{ pageSize }} 条/页 ▾</view>
      </picker>
      <view class="pg-nav">
        <view class="pg-btn {{ page <= 1 ? 'disabled' : '' }}" bindtap="onPrevPage">上一页</view>
        <text class="pg-info">{{ page }}/{{ totalPages }}</text>
        <view class="pg-btn {{ page >= totalPages ? 'disabled' : '' }}" bindtap="onNextPage">下一页</view>
      </view>
    </view>
  </view>
</view>

<t-popup visible="{{ showForm }}" placement="bottom" bind:visible-change="closeForm">
  <view class="form-panel">
    <view class="form-title">{{ formMode === 'edit' ? '编辑客户' : '新增客户' }}</view>
    <view class="form-field">
      <text class="form-label">名称</text>
      <input class="form-input" value="{{ form.name }}" data-field="name" placeholder="必填" bindinput="onFormField" />
    </view>
    <view class="form-field">
      <text class="form-label">联系人</text>
      <input class="form-input" value="{{ form.contact }}" data-field="contact" placeholder="选填" bindinput="onFormField" />
    </view>
    <view class="form-field">
      <text class="form-label">电话</text>
      <input class="form-input" value="{{ form.phone }}" data-field="phone" type="number" placeholder="选填" bindinput="onFormField" />
    </view>
    <view class="form-field">
      <text class="form-label">地址</text>
      <input class="form-input" value="{{ form.address }}" data-field="address" placeholder="选填" bindinput="onFormField" />
    </view>
    <view class="form-field">
      <text class="form-label">状态</text>
      <picker mode="selector" range="{{ statusOptions }}" range-key="label" value="{{ statusIndex }}" bindchange="onStatusChange">
        <view class="form-picker">{{ statusOptions[statusIndex].label }} ▾</view>
      </picker>
    </view>
    <t-button theme="primary" block loading="{{ submitting }}" bindtap="submitForm">
      {{ formMode === 'edit' ? '保存' : '新增' }}
    </t-button>
  </view>
</t-popup>
